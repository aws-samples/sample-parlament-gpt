# Exact pins, deliberately: the container must run the versions the tests ran against.
# gateway.py additionally reaches into a private strands accessor (_is_session_active),
# so an unpinned strands-agents could silently change session-recovery behaviour (threat
# model T7). Upgrades are a deliberate change: bump here AND in pyproject.toml, re-run
# `make test-agent`, and re-read gateway.py's lifecycle notes.
strands-agents==1.52.0
strands-agents-tools==0.8.6
bedrock-agentcore==1.22.0
httpx==0.28.1
boto3==1.43.74
