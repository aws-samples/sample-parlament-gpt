# Root config reference (no secrets). Per-deployable templates live in agent/ and frontend/.
AWS_REGION=eu-central-1
BEDROCK_MODEL_ID=global.anthropic.claude-sonnet-4-6
DIP_FQDN=search.dip.bundestag.de
DIP_SECRET_NAME=parlamentgpt/dip-api-key

# Set after `make deploy` from CDK outputs:
# GUARDRAIL_ID=
# GUARDRAIL_VERSION=
# DIP_API_KEY_SECRET_ARN=
# AGENT_RUNTIME_ARN=
